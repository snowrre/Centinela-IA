# ============================================================
# Centinela IA - Requirements
# Python 3.11 recomendado (ver nota de compatibilidad abajo)
# GTX 1060 6GB | CUDA 13.2 | Driver 581.57
# ============================================================

# --- Interfaz ---
streamlit>=1.35.0

# --- Visión por Computadora ---
# opencv-python ya instalado (4.13.0.92), se omite reinstalación
opencv-python>=4.13.0

# --- Pose & Face Landmarks (ya instalado 0.10.33) ---
mediapipe>=0.10.21

# --- Numerical (ya instalado 2.4.3) ---
numpy>=1.26.0

# --- Cliente Ollama ---
ollama>=0.3.3

# --- ONNX Runtime con soporte GPU (GTX 1060 = CUDA EP) ---
# NOTA: onnxruntime-gpu 1.18+ requiere CUDA 11.8-12.x
# CUDA 13.2 no tiene wheel oficial aún → usamos onnxruntime CPU
# como fallback hasta que salga soporte oficial para CUDA 13
onnxruntime>=1.18.0

# --- Interfaz & Utils ---
supabase>=2.4.0
python-dotenv>=1.0.0
Pillow>=10.3.0
aiohttp>=3.9.0

# --- WebRTC Stream (v3.0) ---
# Reemplaza cv2.VideoCapture para streaming remoto via Ngrok
streamlit-webrtc>=0.47.0
av>=10.0.0
streamlit-autorefresh>=0.0.1
